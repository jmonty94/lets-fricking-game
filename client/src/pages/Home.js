import React from 'react';

const Home = () => {

  return (
    <main>
        <div>
            {/* Only should render upon Header button click / First Page Load */}
        <h1>Home</h1>
        </div>
    </main>
  );
};

export default Home;
